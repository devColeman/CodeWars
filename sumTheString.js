function sumStr(a,b) {
    // P: givne two strings
    // R: return the sum of the two string if both are number if there is no number count
    // count it as a zero
    // E: '10' '53' => 63
    // P: use Number on the both of them
    return (Number(a) + Number(b)).toString()
    
  }