function between(a, b) {
    // P: given two numbes
    // R: return a array
    // E: 3,5 => [3,4,5]
    // P: use a for loop to push into a arrayh
     console.log(a)
     console.log(b)
     let k = []
     for(let i = a; i <= b; i++){
       k.push(i)
     }
     
    return k
   }