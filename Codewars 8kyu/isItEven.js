function testEven(n) {
    // P: given integar
    // R: return either true or false
    // E: 0 = true, .5 = false
    // P: i could use a % 2 === 0 to check if its even
    
    return n % 2 === 0 ? true : false;
  }