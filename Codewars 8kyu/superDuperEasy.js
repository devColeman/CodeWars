function problem(x){
    // P: given a integer
    // R: return a number
    // E: 5 => 256
    // P: use * then add 6
    
     if ( typeof x === 'number') {
       return (x * 50) + 6
      
    }else {
       return "Error";
    }
   
  }

  // test