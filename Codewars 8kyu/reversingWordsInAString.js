function reverse(string){
    // P: given a string
    // R: return a string revered
    // E: 'Hello Bob' => 'Bob Hello'
    // P:  split the string bye the space then reverse them
  
 return string.split(' ').reverse().join(' ')
 
}