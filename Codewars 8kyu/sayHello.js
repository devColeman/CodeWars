function sayHello(name) {
    // P: given a string 
    // R: return a string with the given string inside
    // E: 'devin' => 'Hello, devin'
    // P: use a template literal to add the name
  return `Hello, ${name}`
}