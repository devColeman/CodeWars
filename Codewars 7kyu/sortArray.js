// input: names - unsorted array
// output: sorted array
sortme = function( names ){
    // P: given a array
    // R: return a array
    // E: ['one', 'two', 'three' ] => ["one", "three", "two"]
    return names.sort()
  }